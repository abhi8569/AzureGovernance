# =============================================================================
# Enterprise Access Intelligence Platform (EAIP) — Python dependencies
# Install: pip install -r requirements.txt
# =============================================================================

# --- Azure Identity & Auth ------------------------------------------------
msal>=1.28.0
azure-identity>=1.16.0

# --- Microsoft Graph ------------------------------------------------------
msgraph-sdk>=1.4.0

# --- Azure Management SDKs -----------------------------------------------
azure-mgmt-authorization>=4.0.0
azure-mgmt-resourcegraph>=8.0.0
azure-mgmt-resource>=23.0.0
azure-mgmt-resource-subscriptions>=1.0.0b2
azure-mgmt-managementgroups>=1.0.0
azure-mgmt-keyvault>=10.3.0
azure-mgmt-storage>=21.1.0
azure-mgmt-sql>=3.0.0
azure-mgmt-cosmosdb>=9.5.0
azure-mgmt-network>=26.0.0
azure-storage-file-datalake>=12.15.0

# --- Azure Key Vault Data Plane -------------------------------------------
azure-keyvault-keys>=4.9.0
azure-keyvault-secrets>=4.8.0
azure-keyvault-certificates>=4.8.0

# --- Analysis Services (XMLA) — optional, Windows-only --------------------
# pyadomd>=0.3.0  # Requires MSOLAP COM provider; install manually if needed

# --- Database & Analytics -------------------------------------------------
duckdb>=1.0.0
sqlalchemy>=2.0.0
duckdb-engine>=0.13.0
pyarrow>=16.0.0
pandas>=2.2.0

# --- Networking & Resilience ----------------------------------------------
tenacity>=8.3.0
httpx>=0.27.0

# --- Configuration & Validation -------------------------------------------
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Logging --------------------------------------------------------------
structlog>=24.1.0

# --- SQL Server (pure Python — no ODBC driver needed) ---------------------
mssql-python>=0.5.0

# --- ODBC (optional fallback — only if you prefer pyodbc) -----------------
# pyodbc>=5.1.0  # Requires ODBC Driver 18 installed at OS level

# --- Dev / Test -----------------------------------------------------------
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
ruff>=0.4.0
